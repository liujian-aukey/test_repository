# 项目管理系统 需求文档

**文档版本：** V1.0  
**编写日期：** 2026-04-23  
**编写人：** 产品经理  
**文档状态：** 草稿

---

## 目录

1. [项目背景](#1-项目背景)
2. [术语与缩写](#2-术语与缩写)
3. [系统概述](#3-系统概述)
4. [功能需求](#4-功能需求)
   - 4.1 [登录与退出功能](#41-登录与退出功能)
   - 4.2 [菜单与导航功能](#42-菜单与导航功能)
   - 4.3 [项目概况模块](#43-项目概况模块)
   - 4.4 [需求文档管理模块](#44-需求文档管理模块)
5. [非功能需求](#5-非功能需求)
6. [页面流程图](#6-页面流程图)
7. [数据字典](#7-数据字典)
8. [约束与假设](#8-约束与假设)

---

## 1. 项目背景

### 1.1 背景说明

当前各产品项目的文件（如产品原型、需求文档等）分散存放于不同的管理工具或存储位置，缺乏统一的规范与标准，导致以下问题：

- 不同项目使用不同的文件存放路径或管理系统，造成跨团队协作困难；
- 文档版本管理混乱，难以追溯历史版本；
- 文件查找效率低，信息检索成本高；
- 无法统一掌握各项目的整体概况与进展。

### 1.2 建设目标

建设一套统一的 **项目管理系统**，实现对各产品项目文件的集中、规范化管理，具体目标如下：

- 统一项目信息的录入与维护入口；
- 集中管理各项目的需求文档，支持上传、预览、下载、更新与删除；
- 提供清晰的目录结构，便于快速定位所需文档；
- 具备用户身份认证机制，保障系统安全访问。

---

## 2. 术语与缩写

| 术语 / 缩写 | 说明 |
|---|---|
| 项目 | 一个产品研发项目的整体集合，包含基本信息与相关文档 |
| 版本号 | 项目当前所处的迭代版本标识，如 V1.0、V2.1 |
| 需求文档 | 与项目相关的产品文档文件，包括但不限于产品原型、PRD、说明文档等 |
| 目录树 | 按"项目名称 → 版本号 → 需求文档"层级组织的树形结构导航 |
| 标签页 | 右侧内容区以选项卡形式呈现的页面载体 |
| 侧边栏 | 系统左侧展示菜单层级的导航区域 |

---

## 3. 系统概述

### 3.1 系统名称

**项目管理系统**

### 3.2 系统整体布局

系统采用经典的「左侧导航 + 右侧内容区」布局：

```
┌──────────────────────────────────────────────────────────┐
│  顶部栏（系统名称 + 右上角用户头像/昵称）                       │
├────────────────┬─────────────────────────────────────────┤
│                │  [标签页1] [标签页2] ...                  │
│  侧边栏         │─────────────────────────────────────────│
│  （一/二级菜单） │                                          │
│  （可折叠展开）  │         当前标签页内容区域                  │
│                │                                          │
└────────────────┴─────────────────────────────────────────┘
```

- **侧边栏**：展示系统的一级与二级菜单，支持手动折叠与展开；
- **顶部栏**：展示系统名称；右上角展示当前登录用户的头像（取昵称首字符）与昵称；
- **内容区**：以标签页形式承载各二级菜单对应的页面，标签页名称与二级菜单名称一致。

### 3.3 菜单结构

| 一级菜单 | 二级菜单 | 标签页名称 |
|---|---|---|
| 项目管理 | 项目概况 | 项目概况 |
| 项目管理 | 需求文档管理 | 需求文档管理 |

---

## 4. 功能需求

### 4.1 登录与退出功能

#### 4.1.1 登录功能

**功能描述：**  
系统设置登录入口，用户须通过正确的账号密码完成身份验证后，方可进入系统。

**交互说明：**

1. 未登录状态下访问系统任意页面，自动重定向至登录页；
2. 登录页包含以下输入项：
   - 账号输入框（必填）
   - 密码输入框（必填，输入内容以密文显示）
   - 登录按钮
3. 点击登录按钮后进行表单校验：
   - 账号或密码为空时，给出相应提示信息；
   - 账号或密码不正确时，页面提示"账号或密码错误，请重新输入"；
   - 校验通过后，跳转至系统主页面（默认进入"项目概况"页）。

**初始账号信息（内置）：**

| 字段 | 值 |
|---|---|
| 登录账号 | admin |
| 登录密码 | 12345 |
| 昵称 | 系统管理员 |

**登录状态保持：**  
登录成功后，登录状态可维持 **5 小时**；超时后需重新登录。

#### 4.1.2 退出登录功能

**功能描述：**  
已登录状态下，用户可通过页面右上角的用户入口主动退出登录。

**交互说明：**

1. 页面右上角展示用户头像（取昵称的第一个字符作为头像文字）与昵称；
2. 点击头像区域，弹出下拉菜单，显示"退出登录"选项；
3. 点击"退出登录"后：
   - 清除当前登录状态及 Session/Token；
   - 跳转回系统登录页面。

---

### 4.2 菜单与导航功能

#### 4.2.1 侧边栏菜单

**功能描述：**  
系统左侧设有侧边栏，用于展示系统的分级菜单。

**交互说明：**

1. 侧边栏展示两级菜单，一级菜单可点击展开或收起其下的二级菜单；
2. 侧边栏整体支持折叠与展开：
   - 折叠状态：侧边栏宽度收窄，仅显示一级菜单图标；
   - 展开状态：侧边栏展示完整的一级菜单名称和二级菜单列表；
3. 点击某一二级菜单时，右侧内容区以标签页的形式打开对应页面，标签页名称与该二级菜单名称相同；
4. 同一个二级菜单被多次点击时，不重复打开新标签页，而是激活已有标签页；
5. 用户可关闭标签页，关闭后默认激活相邻标签页。

---

### 4.3 项目概况模块

**所属菜单：** 项目管理 → 项目概况

#### 4.3.1 页面布局

页面采用"顶部操作区 + 数据表格"的布局结构：

- 顶部操作区：提供"查询"和"新增"操作入口；
- 数据表格：分页展示项目信息列表。

#### 4.3.2 项目信息字段

| 字段名 | 说明 | 是否必填 |
|---|---|---|
| 项目名称 | 项目的名称 | 是 |
| 版本号 | 项目版本标识，如 V1.0 | 是 |
| 项目经理 | 负责该项目的项目经理姓名 | 是 |
| 需求部门 | 提出需求的部门名称 | 否 |
| 描述 | 项目的简要描述说明 | 否 |
| 需求文档 | 与项目关联的需求文档文件（支持多个） | 否 |

#### 4.3.3 查询功能

**功能描述：**  
支持按条件对项目列表进行模糊搜索。

**交互说明：**

1. 查询区域位于表格上方，包含以下筛选条件输入项：
   - 项目名称（模糊匹配）
   - 项目经理（模糊匹配）
   - 版本号（模糊匹配）
   - 需求部门（模糊匹配）
2. 各筛选条件可单独或组合使用；
3. 点击"查询"按钮后，表格刷新并展示符合条件的项目列表；
4. 支持清空条件重置为全量查询。

#### 4.3.4 新增项目功能

**功能描述：**  
支持新增一条项目信息记录。

**交互说明：**

1. 点击"新增"按钮，弹出"新增项目"对话框；
2. 对话框包含项目信息的所有字段，其中项目名称、版本号、项目经理为必填项；
3. 需求文档字段通过上传文件的方式录入，支持上传多个文件；
4. 表单校验：必填项为空时，字段下方给出红字提示；
5. 点击"确认"按钮，校验通过后保存数据，关闭弹框并刷新表格；
6. 点击"取消"按钮或关闭对话框图标，关闭弹框，不保存数据。

#### 4.3.5 修改项目功能

**功能描述：**  
支持修改已有项目的信息。

**交互说明：**

1. 在项目列表表格的"操作"列点击"修改"按钮，弹出"修改项目"对话框；
2. 对话框自动回填当前项目的已有信息；
3. 需求文档字段以标签块（Tag）的形式展示当前项目已关联的文件，每个标签块可单独删除；支持继续上传新文件；
4. 必填项校验与新增逻辑一致；
5. 点击"确认"按钮，保存修改并刷新表格；
6. 点击"取消"按钮或关闭弹框，不保存修改。

#### 4.3.6 删除项目功能

**功能描述：**  
支持删除已有项目信息记录。

**交互说明：**

1. 在项目列表表格的"操作"列点击"删除"按钮；
2. 弹出确认对话框，提示文案示例："确定要删除该项目吗？删除后数据将无法恢复。"；
3. 点击"确认删除"按钮后，执行删除操作并刷新表格；
4. 点击"取消"则关闭弹框，不执行删除。

#### 4.3.7 需求文档跳转功能

**功能描述：**  
项目列表中的需求文档列提供跳转入口，可快速定位至对应文档。

**交互说明：**

1. 项目信息表格的"需求文档"列展示该项目已关联的文档文件名（或文件数量）；
2. 每个文档名称作为可点击的超链接入口；
3. 点击后跳转至"需求文档管理"标签页，并自动展开目录树至对应项目和版本节点，同时在右侧文档表格中高亮展示对应文档记录。

---

### 4.4 需求文档管理模块

**所属菜单：** 项目管理 → 需求文档管理

#### 4.4.1 页面布局

页面采用"左侧目录树 + 右侧文档表格"的左右分栏布局：

```
┌─────────────────────┬────────────────────────────────────┐
│                     │                                    │
│  需求文档目录（左侧）  │      文档列表表格 / 空状态（右侧）      │
│                     │                                    │
│  项目A              │                                    │
│  ├── V1.0           │                                    │
│  │   └── 文档1.docx │                                    │
│  └── V2.0           │                                    │
│  项目B              │                                    │
│  └── V1.0           │                                    │
│                     │                                    │
└─────────────────────┴────────────────────────────────────┘
```

#### 4.4.2 需求文档目录树

**功能描述：**  
左侧目录树按照"项目名称 → 版本号 → 需求文档"三级层级展示所有已录入的文档目录结构。

**交互说明：**

1. 默认展开所有一级节点（项目名称）；
2. 点击版本号节点，可展开/收起其下的文档文件列表；
3. 点击某一目录节点（版本号节点或文件节点），右侧文档表格联动刷新展示对应内容；
4. 被选中的节点高亮显示。

#### 4.4.3 文档表格展示（有文档状态）

**功能描述：**  
当选中的目录节点下存在文档时，右侧展示文档列表表格。

**表格字段如下：**

| 字段名 | 说明 |
|---|---|
| 文档名称 | 上传文件的原始文件名 |
| 项目名称 | 所属项目名称 |
| 版本号 | 所属版本号 |
| 项目经理 | 项目负责人姓名 |
| 需求部门 | 需求提出部门 |
| 上传人 | 执行上传操作的用户账号/昵称 |
| 更新时间 | 文件最后一次上传或更新的时间，精确至秒 |

**操作列功能：**

| 操作按钮 | 功能说明 |
|---|---|
| 预览 | 在右侧抽屉/弹窗中打开文件预览 |
| 下载 | 将文件直接下载到本地 |
| 更新 | 弹出文件选择框，上传新文件替换当前文件，完成后刷新表格 |
| 删除 | 弹出确认对话框，确认后删除该文件记录，刷新表格 |

#### 4.4.4 文件预览规则

**支持预览的文件格式：**

| 格式 | 说明 |
|---|---|
| `.rp` | Axure 原型文件 |
| `.xlsx` / `.xls` | Excel 表格文件 |
| `.docx` / `.doc` | Word 文档 |
| `.html` / `.htm` | HTML 页面文件 |
| `.pptx` / `.ppt` | PowerPoint 演示文件 |
| `.pdf` | PDF 文档 |
| `.zip` | 压缩文件（内容列表预览） |

**不支持预览时的处理：**  
若文件格式不在上述支持列表中，预览窗口内展示提示文案：

> "不支持当前文件格式预览，请下载后在本地打开"

#### 4.4.5 文档表格展示（无文档状态）

**功能描述：**  
当选中的目录节点下暂无文档时，右侧内容区展示空状态提示。

**交互说明：**

1. 右侧展示文案提示："当前目录下无需求文档"；
2. 文案下方提供"立即上传"按钮；
3. 点击"立即上传"按钮，弹出文件选择框，选择本地文件后上传至当前目录；
4. 上传完成后，自动刷新右侧文档表格，切换至有文档状态展示。

#### 4.4.6 文档删除功能

**交互说明：**

1. 点击操作列中的"删除"按钮；
2. 弹出确认对话框，提示文案示例："确定要删除该文档吗？删除后数据将无法恢复。"；
3. 点击"确认删除"，执行删除并刷新表格；
4. 点击"取消"则关闭弹框，不执行操作。

---

## 5. 非功能需求

### 5.1 性能需求

| 指标 | 要求 |
|---|---|
| 页面加载时长 | 常规页面首次加载时间不超过 3 秒 |
| 文件上传大小 | 单个文件上传大小建议不超过 100 MB |
| 文件预览响应 | 预览窗口打开响应时间不超过 5 秒 |
| 列表分页 | 默认每页展示 10 条，支持调整为 20 / 50 条 |

### 5.2 安全需求

| 指标 | 要求 |
|---|---|
| 访问控制 | 未登录用户无法访问系统任何功能页面 |
| 登录状态 | 登录 Token/Session 有效期为 5 小时，过期后自动失效并跳转登录页 |
| 密码传输 | 登录密码在传输过程中应进行加密处理（如 HTTPS + 摘要加密） |

### 5.3 兼容性需求

| 指标 | 要求 |
|---|---|
| 浏览器 | 支持 Chrome、Edge、Firefox 最新两个主要版本 |
| 分辨率 | 支持 1280×720 及以上屏幕分辨率的正常展示 |
| 响应式 | 以桌面端为主，不要求移动端自适应 |

### 5.4 可用性需求

- 界面操作逻辑清晰，关键操作（删除、覆盖更新）须有二次确认提示；
- 上传、查询、保存等操作应有相应的加载状态和结果反馈（成功/失败提示）；
- 表单必填项须有明确的校验提示，避免用户迷惑。

---

## 6. 页面流程图

### 6.1 登录流程

```
访问系统
   │
   ▼
未登录？──是──► 跳转至登录页
   │                │
   否               ▼
   │         输入账号/密码
   │                │
   ▼         账号密码正确？──否──► 提示"账号或密码错误"
进入主页          │
               是
               │
               ▼
          进入系统主页（默认展示"项目概况"）
```

### 6.2 项目概况操作流程

```
项目概况页
   │
   ├──► 查询：输入条件 → 点击查询 → 刷新列表
   │
   ├──► 新增：点击新增 → 填写表单 → 上传文档 → 确认保存 → 刷新列表
   │
   ├──► 修改：点击修改 → 回填信息 → 编辑 → 确认保存 → 刷新列表
   │
   ├──► 删除：点击删除 → 确认弹框 → 确认删除 → 刷新列表
   │
   └──► 文档查看：点击文档链接 → 跳转"需求文档管理" → 定位至对应文档
```

### 6.3 需求文档管理操作流程

```
需求文档管理页
   │
   ├── 左侧目录树
   │     └── 点击节点 → 右侧刷新文档列表
   │
   └── 右侧文档区域
         │
         ├── 有文档 ─► 显示文档列表
         │               ├── 预览：打开预览窗口（支持格式则展示，否则提示）
         │               ├── 下载：直接下载文件
         │               ├── 更新：上传新文件替换 → 刷新列表
         │               └── 删除：确认弹框 → 确认删除 → 刷新列表
         │
         └── 无文档 ─► 显示"当前目录下无需求文档" + "立即上传"按钮
                           └── 点击立即上传 → 选择文件上传 → 刷新列表（切换至有文档状态）
```

---

## 7. 数据字典

### 7.1 项目信息表（Project）

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|---|---|---|---|---|
| id | String/UUID | - | 是 | 项目唯一标识 |
| project_name | String | 100 | 是 | 项目名称 |
| version | String | 50 | 是 | 版本号 |
| manager | String | 50 | 是 | 项目经理姓名 |
| department | String | 100 | 否 | 需求部门 |
| description | String | 500 | 否 | 项目描述 |
| create_time | DateTime | - | 是 | 创建时间 |
| update_time | DateTime | - | 是 | 最后修改时间 |

### 7.2 需求文档表（Document）

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|---|---|---|---|---|
| id | String/UUID | - | 是 | 文档唯一标识 |
| project_id | String/UUID | - | 是 | 关联项目 ID |
| file_name | String | 255 | 是 | 文件原始名称 |
| file_path | String | 500 | 是 | 文件存储路径 |
| file_type | String | 20 | 否 | 文件扩展名 |
| uploader | String | 50 | 是 | 上传人账号/昵称 |
| upload_time | DateTime | - | 是 | 上传时间 |
| update_time | DateTime | - | 是 | 最后更新时间 |

### 7.3 用户表（User）

| 字段名 | 类型 | 长度 | 必填 | 说明 |
|---|---|---|---|---|
| id | String/UUID | - | 是 | 用户唯一标识 |
| username | String | 50 | 是 | 登录账号 |
| password | String | 255 | 是 | 登录密码（加密存储） |
| nickname | String | 50 | 是 | 用户昵称 |

---

## 8. 约束与假设

1. **用户规模：** 当前阶段仅设置一个管理员账号（admin），后续可扩展多用户及权限管理；
2. **文件存储：** 上传文件的实际存储方式（本地磁盘 / 对象存储）由开发团队根据部署环境决定；
3. **文件预览：** `.rp`（Axure 原型）文件预览需前端集成相应解析方案，其可行性由开发团队评估；
4. **数据持久化：** 系统需配套数据库进行数据持久化存储，数据库类型由开发团队选型；
5. **网络环境：** 系统在内网或局域网环境中部署使用；
6. **本文档范围：** 本文档描述的功能范围为 V1.0 版本，后续迭代需求另行补充。

---

*文档结束*
